<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.secoco">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeCoCo">
        <activity android:name=".CambioContrasena"></activity>
        <activity android:name=".usuarios.etdacovid.ETDAInicio" />
        <activity android:name=".usuarios.erecovid.EREInicio" />
        <activity android:name=".usuarios.erccovid.ERCInicio" />
        <activity android:name=".usuarios.persona.Map" />
        <activity android:name=".Registro" />
        <activity android:name=".Ingreso">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>